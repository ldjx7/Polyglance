# Polyglance v0.0.7-beta.2

Polyglance v0.0.7-beta.2 修复了 macOS 设置面板中「立即检查更新」按钮无响应的问题。

## 核心修复

- **macOS 设置检查更新响应修复**：
  - 修复 SwiftUI `@NSApplicationDelegateAdaptor` 内部适配器导致 `NSApp.delegate` 向下类型转换失败的问题。
  - 将 `AppUpdater` 单例化管理，确保设置界面的检查更新按钮与菜单栏更新动作统一调用常驻更新器，杜绝临时实例被 ARC 析构导致的按钮无响应。
