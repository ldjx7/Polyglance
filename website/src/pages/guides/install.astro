---
import PageLayout from '../../layouts/PageLayout.astro';
---

<PageLayout title="安装指南" description="Polyglance 在 macOS 与 Windows 上的安装与权限配置说明" lang="zh-CN">
  <div class="page-header">
    <h1 class="page-title">安装与配置指南</h1>
    <p class="page-desc">简单几步完成安装，开启无缝屏幕翻译与捕捉体验。</p>
  </div>

  <!-- macOS Card -->
  <div class="card">
    <div style="display:flex; align-items:center; gap:0.6rem; margin-bottom:1rem;">
      <svg style="width:20px; height:20px; fill:currentColor;" viewBox="0 0 24 24"><path d="M18.71 19.5c-.83 1.24-1.71 2.45-3.05 2.47-1.34.03-1.77-.79-3.29-.79-1.53 0-2 .77-3.27.82-1.31.05-2.3-1.32-3.14-2.53C4.25 17 2.94 12.45 4.7 9.39c.87-1.52 2.43-2.48 4.12-2.51 1.28-.02 2.5.87 3.29.87.78 0 2.26-1.07 3.81-.91.65.03 2.47.26 3.64 1.98-.09.06-2.17 1.28-2.15 3.81.03 3.02 2.65 4.03 2.68 4.04-.03.07-.42 1.44-1.38 2.83M15.97 4.37c.62-.75 1.04-1.8 1.01-2.87-.96.04-2.12.64-2.8 1.44-.59.68-1.11 1.76-.97 2.8 1.07.08 2.14-.54 2.76-1.37z"/></svg>
      <h2 class="card-title" style="margin-bottom:0;">macOS 安装与系统权限</h2>
    </div>

    <div style="font-size:0.95rem; color:var(--text-2); line-height:1.7;">
      <h3 style="font-size:1.05rem; color:var(--text-1); margin:1rem 0 0.5rem;">1. 安装</h3>
      <p>前往 <a href="/download" style="color:var(--accent); text-decoration:underline;">下载页面</a> 获取最新的 <code>.dmg</code> 文件，双击打开后将 <strong>Polyglance</strong> 拖入应用程序 (Applications) 目录。</p>

      <h3 style="font-size:1.05rem; color:var(--text-1); margin:1.25rem 0 0.5rem;">2. 首次启动提示已损坏或无法打开的解决办法</h3>
      
      <div style="background:var(--bg-subtle); border-left:3px solid var(--accent); padding:0.75rem 1rem; margin:0.75rem 0 1rem; border-radius:4px; font-size:0.88rem; color:var(--text-2);">
        <strong style="color:var(--text-1);">为什么 macOS 会拦截？</strong><br />
        Polyglance 为 100% 透明开源的免费效率工具，未向 Apple 购买每年 99 美元的付费开发者签名与公证。macOS Gatekeeper 安全机制会对从网络下载的未签名应用添加下载隔离属性，进而弹出「已损坏，打不开。你应该将它移到废纸篓」或「来自身份不明的开发者」的拦截警告。代码在 GitHub 全开源，安全无害，可通过以下三种方式正常放行：
      </div>

      <div style="display:flex; flex-direction:column; gap:1rem; margin:1rem 0;">
        <div style="background:var(--bg-subtle); border:1px solid var(--border-subtle); border-radius:8px; padding:1rem 1.15rem;">
          <h4 style="font-size:0.95rem; color:var(--text-1); margin-bottom:0.4rem; display:flex; align-items:center; gap:0.4rem;">
            <span style="background:var(--accent); color:#fff; font-size:0.75rem; padding:0.1rem 0.4rem; border-radius:4px; font-weight:700;">方法一</span>
            在「系统设置」中放行（推荐）
          </h4>
          <ol style="padding-left:1.25rem; font-size:0.9rem; line-height:1.6; color:var(--text-2); margin-top:0.4rem;">
            <li><strong>关闭报错弹窗</strong>：点击弹窗中的「完成」或「取消」（切记不要点击「移到废纸篓」）。</li>
            <li><strong>打开隐私与安全性设置</strong>：点击屏幕左上角  苹果图标 → 打开「系统设置」(System Settings)，在左侧边栏找到并点击「隐私与安全性」(Privacy &amp; Security)。</li>
            <li><strong>点击「仍要打开」</strong>：向下滑动到「安全性」区域，会看到提示「已阻止使用 Polyglance，因为来自身份不明的开发者」，点击右侧的「仍要打开」(Open Anyway) 按钮，输入开机密码或触控 ID 确认，在随后弹出的确认对话框中点击「打开」即可正常启动。</li>
          </ol>
        </div>

        <div style="background:var(--bg-subtle); border:1px solid var(--border-subtle); border-radius:8px; padding:1rem 1.15rem;">
          <h4 style="font-size:0.95rem; color:var(--text-1); margin-bottom:0.4rem; display:flex; align-items:center; gap:0.4rem;">
            <span style="background:var(--text-2); color:#fff; font-size:0.75rem; padding:0.1rem 0.4rem; border-radius:4px; font-weight:700;">方法二</span>
            访达快捷右键打开（更快速）
          </h4>
          <ol style="padding-left:1.25rem; font-size:0.9rem; line-height:1.6; color:var(--text-2); margin-top:0.4rem;">
            <li>打开「访达」(Finder)，进入「应用程序」(Applications) 文件夹。</li>
            <li>找到 Polyglance，按住键盘上的 <kbd style="background:var(--bg-card); border:1px solid var(--border-subtle); padding:0.1rem 0.35rem; border-radius:4px;">Control</kbd> 键并点击它（或者直接鼠标右键点击）。</li>
            <li>在弹出的上下文菜单中选择「打开」。</li>
            <li>此时系统弹出的提示框中会多出一个直接的「打开」按钮，点击即可正常运行并永久加入白名单。</li>
          </ol>
        </div>

        <div style="background:var(--bg-subtle); border:1px solid var(--border-subtle); border-radius:8px; padding:1rem 1.15rem;">
          <h4 style="font-size:0.95rem; color:var(--text-1); margin-bottom:0.4rem; display:flex; align-items:center; gap:0.4rem;">
            <span style="background:var(--text-2); color:#fff; font-size:0.75rem; padding:0.1rem 0.4rem; border-radius:4px; font-weight:700;">方法三</span>
            终端执行命令移除隔离属性（彻底解决提示已损坏）
          </h4>
          <p style="font-size:0.9rem; color:var(--text-2); margin-top:0.3rem;">
            若打开时系统直接提示「已损坏，你应该将它移到废纸篓」，请打开系统「终端」(Terminal)，复制并运行以下命令清除隔离属性：
          </p>
          <div style="background:var(--bg-card); border:1px solid var(--border-subtle); border-radius:6px; padding:0.6rem 0.85rem; margin-top:0.5rem; font-family:var(--font-code); font-size:0.86rem; color:var(--text-1);">
            <code>xattr -cr /Applications/Polyglance.app</code>
          </div>
        </div>
      </div>

      <h3 style="font-size:1.05rem; color:var(--text-1); margin:1.25rem 0 0.5rem;">3. 系统权限授予</h3>
      <p>为了让划词取词与截图功能正常工作，需在 <em>系统设置 → 隐私与安全性</em> 中授予：</p>
      <ul style="padding-left:1.5rem; margin-top:0.5rem;">
        <li><strong>辅助功能 (Accessibility)</strong>：用于全局快捷键与划词选区获取。</li>
        <li><strong>屏幕录制 (Screen Recording)</strong>：用于截图、OCR 识别、长截图与录屏。</li>
      </ul>
    </div>
  </div>

  <!-- Windows Card -->
  <div class="card">
    <div style="display:flex; align-items:center; gap:0.6rem; margin-bottom:1rem;">
      <svg style="width:20px; height:20px; fill:currentColor;" viewBox="0 0 24 24"><path d="M0 0h11.377v11.377H0zm12.623 0H24v11.377H12.623zM0 12.623h11.377V24H0zm12.623 0H24V24H12.623z"/></svg>
      <h2 class="card-title" style="margin-bottom:0;">Windows 安装说明</h2>
    </div>

    <div style="font-size:0.95rem; color:var(--text-2); line-height:1.7;">
      <h3 style="font-size:1.05rem; color:var(--text-1); margin:1rem 0 0.5rem;">1. 安装版（推荐）</h3>
      <p>前往 <a href="/download" style="color:var(--accent); text-decoration:underline;">下载页面</a> 获取 <code>Windows-x64-Setup.exe</code> 并双击运行，安装程序会自动为当前用户配置快捷方式并注册自启动支持。</p>

      <h3 style="font-size:1.05rem; color:var(--text-1); margin:1.25rem 0 0.5rem;">2. 便携版</h3>
      <p>前往 <a href="/download" style="color:var(--accent); text-decoration:underline;">下载页面</a> 获取 <code>Windows-x64-Portable.zip</code>，解压至任意目录后直接双击 <code>Polyglance.exe</code> 即可使用。</p>

      <h3 style="font-size:1.05rem; color:var(--text-1); margin:1.25rem 0 0.5rem;">3. SmartScreen 提示</h3>
      <p>首次运行如提示 Windows 已保护你的电脑，点击<strong>更多信息 → 仍要运行</strong>即可。</p>
    </div>
  </div>
</PageLayout>
