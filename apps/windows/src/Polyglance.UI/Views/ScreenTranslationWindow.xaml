<Window x:Class="Polyglance.UI.Views.ScreenTranslationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        MouseDown="OnMouseDown">
    <Border Margin="12" Background="#FAFFFFFF" BorderBrush="#26000000" BorderThickness="1" CornerRadius="16" Padding="16" MinWidth="280" MaxWidth="540">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" Direction="270" ShadowDepth="8" Opacity="0.18" Color="#000000"/>
        </Border.Effect>
        <StackPanel>
            <!-- 头部标题栏 -->
            <DockPanel Margin="0,0,0,12">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#100A84FF" CornerRadius="6" Width="22" Height="22" Margin="0,0,8,0">
                        <TextBlock Text="译" FontWeight="Black" FontSize="11" Foreground="#0A84FF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="智能截屏翻译" FontWeight="Bold" FontSize="13" Foreground="#1E293B" VerticalAlignment="Center"/>
                    <Border Background="#F1F5F9" CornerRadius="4" Padding="6,2" Margin="8,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="自动检测 → 中文" FontSize="10" Foreground="#64748B" FontWeight="Medium"/>
                    </Border>
                </StackPanel>

                <!-- 关闭按钮 -->
                <Button Width="24" Height="24" Background="Transparent" BorderThickness="0" HorizontalAlignment="Right" Click="OnCloseClick" Cursor="Hand">
                    <Path Data="M19,6.41 L17.59,5 L12,10.59 L6.41,5 L5,6.41 L10.59,12 L5,17.59 L6.41,19 L12,13.41 L17.59,19 L19,17.59 L13.41,12 Z" Fill="#94A3B8" Width="10" Height="10" Stretch="Uniform"/>
                </Button>
            </DockPanel>

            <!-- 原文区域卡片 -->
            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="10,8" Margin="0,0,0,10">
                <TextBlock x:Name="TxtSource" Text="Source Text" FontSize="12" Foreground="#64748B" TextWrapping="Wrap" LineHeight="18"/>
            </Border>

            <!-- 译文结果卡片 -->
            <Border Background="#FFFFFF" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="10" Padding="12,10" Margin="0,0,0,14">
                <TextBlock x:Name="TxtTarget" Text="Target Translated Text" FontSize="14" FontWeight="SemiBold" Foreground="#0F172A" TextWrapping="Wrap" LineHeight="22"/>
            </Border>

            <!-- 底部操作按钮 -->
            <DockPanel>
                <Button Content="贴出原图 (Pin)" Padding="12,6" Background="#F1F5F9" Foreground="#334155" BorderThickness="0" FontWeight="Medium" FontSize="12" Cursor="Hand" Click="OnPinOriginalClick"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="复制译文" Padding="16,6" Background="#0A84FF" Foreground="White" BorderThickness="0" FontWeight="SemiBold" FontSize="12" Cursor="Hand" Click="OnCopyTranslationClick">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="8" Direction="270" ShadowDepth="2" Opacity="0.25" Color="#0A84FF"/>
                        </Button.Effect>
                    </Button>
                </StackPanel>
            </DockPanel>
        </StackPanel>
    </Border>
</Window>
